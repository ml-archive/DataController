apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 25
    }
}

dependencies {
    provided project("${datacontroller_project_prefix}datacontroller")
    provided project("${datacontroller_project_prefix}datacontroller-dbflow")
    provided project("${datacontroller_project_prefix}datacontroller-retrofit")
    provided project("${datacontroller_project_prefix}datacontroller-sharedpreferences")
}
